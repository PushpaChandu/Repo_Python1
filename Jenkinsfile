pipeline {
    agent any
    stages {
      stage('Test') {
        steps {
          println "Hello Pushpa"
          sh '''
            #!/bin/basah
            pythion3 --version
          '''
        }
      }
    }
}
